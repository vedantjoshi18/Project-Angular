import { StatusHighlight } from './status-highlight';

describe('StatusHighlight', () => {
  it('should create an instance', () => {
    const directive = new StatusHighlight();
    expect(directive).toBeTruthy();
  });
});
